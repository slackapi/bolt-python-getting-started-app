[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bolt-python-getting-started-app"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "slack-bolt",
]


[tool.setuptools]
py-modules = []
